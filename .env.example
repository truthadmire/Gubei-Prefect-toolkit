# Public feed is opt-in. With both flags false, the app remains fully local.
NEXT_PUBLIC_SHARED_HISTORY_ENABLED=false
SHARED_HISTORY_ENABLED=false

# Supplied by the Neon integration in Vercel Marketplace.
DATABASE_URL=

# Generate a private random value of at least 32 bytes for network hashing.
RATE_LIMIT_SECRET=
